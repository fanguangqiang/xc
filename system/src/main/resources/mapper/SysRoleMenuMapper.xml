<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fan.system.dao.SysRoleMenuDao">

    <resultMap id="BaseResultMap" type="com.fan.system.entity.SysRoleMenu">
        <!--@Table sys_role_menu-->
        <result property="roleId" column="role_id" jdbcType="INTEGER"/>
        <result property="menuId" column="menu_id" jdbcType="INTEGER"/>
    </resultMap>

   <!-- &lt;!&ndash;查询单个&ndash;&gt;
    <select id="queryById" resultMap="BaseResultMap">
        select
          
role_id, menu_id
        from xc.sys_role_menu
        where  = #{}
    </select>

    &lt;!&ndash;查询指定行数据&ndash;&gt;
    <select id="queryAllByLimit" resultMap="BaseResultMap">
        select
          
role_id, menu_id
        from xc.sys_role_menu
        limit #{offset}, #{limit}
    </select>

    &lt;!&ndash;通过实体作为筛选条件查询&ndash;&gt;
    <select id="queryAll" resultMap="BaseResultMap">
        select
          
role_id, menu_id
        from xc.sys_role_menu
        <where>
            <if test="
roleId != null">
                and 
role_id = #{
roleId}
            </if>
            <if test="menuId != null">
                and menu_id = #{menuId}
            </if>
        </where>
    </select>

    &lt;!&ndash;新增所有列&ndash;&gt;
    <insert id="insert" keyProperty="" useGeneratedKeys="true">
        insert into xc.sys_role_menu(
role_id, menu_id)
        values (#{
roleId}, #{menuId})
    </insert>

    &lt;!&ndash;通过主键修改数据&ndash;&gt;
    <update id="update">
        update xc.sys_role_menu
        <set>
            <if test="
roleId != null">
                
role_id = #{
roleId},
            </if>
            <if test="menuId != null">
                menu_id = #{menuId},
            </if>
        </set>
        where  = #{}
    </update>

    &lt;!&ndash;通过主键删除&ndash;&gt;
    <delete id="deleteById">
        delete from xc.sys_role_menu where  = #{}
    </delete>-->

</mapper>